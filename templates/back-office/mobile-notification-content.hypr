<div class="header shipment-Info-header">
    <img class="sidebar-kibo-logo" src="/resources/images/kibo-logo-sidebar.png">
</div>
</div>
<div class="shipment-Title"><b>{{ shipmentTitle }}</b></div>
<table class="mz-printable-order mz-printable-shipment-mobilenotification">
<thead class="header"><tr>
    <th class="shipment-essentials">
        <div class="order-number"><span class="label">{{ labels.orderNo }}</span><span>{{ model.orderNumber }}</span></div>
        <div class="order-number"><span class="label">{{ labels.shipmentNo }}</span><span>{{ model.number }}</span></div>
        <div class="order-number"><span class="label">{{ labels.shipmentType }} </span><span>{{ model.shipmentType }}</span></div>
         <div class="order-number"><span class="label">{{ labels.pickLocation }} </span><span>{{ model.fulfillmentLocationCode }}</span></div>
        <div><span class="shipment-satus"><span class="label">{{ labels.shipmentStatus }} </span><span>{{ model.shipmentStatus }}</span></span></div>
       
    </th>
</tr></thead>

{% if model.items.length > 0 %}
<tbody><tr><td colspan="4">
<div class="shipment-Item-info">{{ labels.shipmentCanceledInfo }} </div>
   <table class="grid">
        <thead><tr>
           <th>{{ labels.item }}</th>
            <th> </th>
            <th>{{ labels.price }}</th>
            <th>{{ labels.qty }}</th>
            <th>{{ labels.total }}</th>
        </tr></thead>
        <tbody>
            {% for item in model.canceledItems %}
            <tr>
                    <td>
                       <img style="height: 100px;" src="{{ item.imageUrl }}">
                    </td>
                    <td>
                        <div class="product-name">
                        <span><b>{{ item.name }}</b></span></br>
                        <span class="label">{{ labels.discount }}{{ item.itemDiscount|currency }}</span></br>
                        <span class="label">{{ labels.shipmentTax }}{{ item.itemTax|currency }}</span>
                        </div>
                    </td>
                    <td>
                        {{ item.actualPrice|currency }}
                    </td>
                    <td>
                        {{ item.quantity }}
                    </td>
                     <td>
                        {{ item.lineItemCost|currency }}
                    </td>
             </tr>
            {% endfor %}
    </tbody></table>
   {% endif %} 
{% if model.items.length > 0 %}
    <tbody><tr><td colspan="4">
    <div class="shipment-Item-info">{{ labels.shipmentFulfillInfo }}</div>
    <table class="grid">
        <thead><tr>
            <th>{{ labels.item }}</th>
            <th> </th>
            <th>{{ labels.price }}</th>
            <th>{{ labels.qty }}</th>
            <th>{{ labels.total }}</th>
        </tr></thead>
        <tbody>
            {% for item in model.items %}
            <tr>
                     <td>
                       <img style="height: 100px;" src="{{ item.imageUrl }}">
                    </td>
                    <td>
                        <div class="product-name">
                        <span><b>{{ item.name }}</b></span></br>
                        <span class="label">{{ labels.discount }}{{ item.itemDiscount|currency }}</span></br>
                        <span class="label">{{ labels.shipmentTax }}{{ item.itemTax|currency }}</span>
                        </div>
                    </td>
                    <td>
                        {{ item.actualPrice|currency }}
                    </td>
                    <td>
                        {{ item.quantity }}
                    </td>
                     <td>
                        {{ item.lineItemCost|currency }}
                    </td>
             </tr>
            {% endfor %}
    </tbody></table>
    {% endif %}
    