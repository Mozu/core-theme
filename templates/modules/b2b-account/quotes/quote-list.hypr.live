{% if model.items %}
        {% for item in model.items %}
        {% include "modules/b2b-account/quotes/quote-item-listing" with model=item %}
        {% endfor %} 
{% else %}
        {{ labels.accountNoWishlists }}
{% endif %}