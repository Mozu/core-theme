/**
 * @class Taco.view.filter.FieldStore
 * a store that allows for dynamically looking up the 
 */


    //Ext.define('Taco.view.filter.FieldStore', {
    //    extend: 'Ext.data.Store',
    //    getFieldRecord: function(config) {
    //        // need to add support for callbacks since the store may have to call the attribute service.
    //        return rec;
    //    },
    //    getById: function(id) {
    //        var rec = this.callParent();
    //        return rec;
    //    }
    //});

