{% if Model|length < 1 %}
	<div class="empty-text">Nothing has been tagged yet.</div>
{% endif %}

<div class="tagcloud">
{% for tag in Model %}
<a href="/blogs/tagged/{{tag.name}}" rel="{{tag.count}}">{{tag.name}}</a>
{% endfor %}
</div>
