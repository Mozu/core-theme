<thead>
  <tr>
    <th class="mz-carttable-header-product">Product</th>
    <th class="mz-carttable-header-price">Price</th>
    <th class="mz-carttable-header-qty">Qty</th>
    <th class="mz-carttable-header-total">Item Total</th>
    <th>&nbsp;</th>
  </tr>
</thead>
<tbody class="mz-carttable-{%if Model.IsEmpty %}isempty{% else %}items{% endif %}">
  {% if not Model.IsEmpty %}
  {% for item in Model.Items %}
  <tr class="mz-carttable-item">
    <td class="mz-carttable-item-product">
      {% if item.Product.mainImage %}
          <img class="mz-carttable-item-image" src="{{ item.Product.mainImage.ImagePath }}?size=50" />
      {% endif %}
      <div class="mz-carttable-item-info-wrap{% if not item.Product.mainImage %} mz-no-image{% endif %}">
        <a class="mz-carttable-item-title" href="{{item.Product.Url}}">{{ item.Product.Name }}</a>
        {% if item.Product.Options %}
        <dl class="mz-propertylist">
          {% for option in item.Product.Options %}
          <dt>{{ option.Name }}</dt>&nbsp;
          <dd>{{ option.Value }}</dd>
          {% endfor %}
        </dl>
        {% endif %}
        <span class="mz-carttable-item-code">
          Product Code: <em>{{ item.Product.ProductCode }}</em>
        </span>
      </div>
    </td>
    <td class="mz-carttable-item-price">
      {% if item.priceIsModified %}
      <span class="mz-carttable-price is-crossedout">{{ item.UnitPrice.BaseAmount|currency }}</span>
      {% endif %}
      <span class="mz-carttable-price{% if item.priceIsModified %} is-saleprice{% endif %}">{{ item.UnitPrice.DiscountedAmount|currency }}</span>
    </td>
    <td  class="mz-carttable-item-qty">
      <input class="mz-carttable-qty-field" type="number" min="1" {% if Model.isLoading %}disabled="disabled"{% endif %} data-mz-cart-item="{{ item.Id }}" data-mz-value="Quantity" value="{{ item.Quantity }}" />
    </td>
    <td class="mz-carttable-item-total">
      <span class="mz-carttable-rowtotal">{{ item.Total|currency }}</span>
    </td>
    <td class="mz-carttable-item-remove">
      <a href="javascript:;" data-mz-action="removeItem" data-mz-cart-item="{{ item.Id }}" class="mz-icon mz-icon-close">Remove</a>
    </td>
  </tr>
{% endfor %}
{% else %}
  <tr class="mz-carttable-noitems">
    <td colspan="5">
      You have no items in your cart.
    </td>
  </tr>
  {% endif %}
</tbody>
<tfoot>
  <tr>
    <td colspan="5" align="right">
      <em class="mz-auxiliary-text">Tax &amp; Shipping Calculated at Checkout</em>
    </td>
  </tr>

  {% for discount in Model.OrderDiscounts %}
    {% if not discount.Excluded %}
  <tr class="mz-carttable-total-row">
    <td class="mz-carttable-total-label" colspan="3" align="right">
        {{ discount.Discount.Name }}:
    </td>
    <td class="mz-carttable-total-wrap" align="right">
      <span class="mz-carttable-rowtotal">- {{ discount.Impact|currency }}</span>
    </td>
    <td></td>
  </tr>
    {% endif %}
  {% endfor %}


  <tr class="mz-carttable-total-row">
    <td class="mz-carttable-total-label" colspan="3" align="right">
      Order Total:
    </td>
    <td class="mz-carttable-total-wrap" align="right">
      <strong>
        <span id="mz-carttable-total" class="mz-carttable-total">{{ Model.DiscountedTotal|currency }}</span>
      </strong>
    </td>
    <td></td>
  </tr>
  <tr>
    <td colspan="4" align ="right">
      <input type="hidden" name="Id" value="{{ Model.Id }}" />
      <button id="cart-checkout" class="mz-button mz-button-large"  {% if Model.IsEmpty %} disabled="disabled" {% endif %} data-mz-action="proceedToCheckout">Proceed to Checkout</button>
    </td>
    <td></td>
  </tr>
</tfoot>